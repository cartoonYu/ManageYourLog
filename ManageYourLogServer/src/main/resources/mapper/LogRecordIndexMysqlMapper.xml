<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.manageyourlogserver.dao.mysql.mapper.LogRecordIndexMysqlMapper">
    <sql id="LogRecordIndex">
        indexId, logRecordId, indexSortIndexId, indexValue, version, createTime, modifyTime
    </sql>

    <insert id="insert" parameterType="org.manageyourlogserver.dao.mysql.entity.LogRecordIndexMysqlEntity">
        insert into LogRecordIndex(indexId, logRecordId, indexSortIndexId, indexValue, version, createTime, modifyTime)
        value(#{indexId}, #{logRecordId}, #{indexSortIndexId}, #{indexValue}, #{version}, #{createTime}, #{modifyTime})
    </insert>

    <select id="selectByIndexValue" resultType="org.manageyourlogserver.dao.mysql.entity.LogRecordIndexMysqlEntity">
        select <include refid="LogRecordIndex"/>
        from LogRecordIndex where indexValue = #{indexValue}
    </select>
</mapper>