store:
  mode: mysql
  load:
    mode: all
  mysql:
    jdbcUrl: jdbc:mysql://localhost:3308/ManageYourLog?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&zeroDateTimeBehavior=convertToNull
    username:
    password:
    driverClassName: com.mysql.cj.jdbc.Driver
query:
  log:
    load:
      mode: http
mariadb:
  baseDir: ../local/base
  dataDir: ../local/data
  port: 3308